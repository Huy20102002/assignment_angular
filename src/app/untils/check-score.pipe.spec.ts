import { CheckScorePipe } from './check-score.pipe';

describe('CheckScorePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckScorePipe();
    expect(pipe).toBeTruthy();
  });
});
